import React, { useEffect } from "react";

const RedirectToken = ({  }) => {
    YaSendSuggestToken(
        'https://support.hobbs-it.ru/', 
        {
           flag: true
        }
     )

  return <div>

  </div>;
};

export default RedirectToken;
